spring:
  application:
    name: SERVICE-REGISTRY

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

server:
  port: 8761

management:
  tracing:
    sampling:
      probability: 1.0
